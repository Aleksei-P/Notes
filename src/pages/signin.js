import React, { useEffect } from "react";

const SignIn = (props) => {
    useEffect(() => {
        document.title = 'Sing In - Notedly';
    });

    return (
        <div>
            <p>Sign Up</p>
        </div>
    );
};

export default SignIn;